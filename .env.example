# Copie este archivo como ".env.local" (no ".env.example") y complete con los datos
# reales de su proyecto de Supabase: Project Settings -> API.
# El archivo .env.local NUNCA se sube a GitHub (está protegido en .gitignore).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
